import { createContext, useReducer } from 'react';

export const TasksContext = createContext([]);

export const GanttContext = createContext([]);

export const OverLordContext = createContext(false);
